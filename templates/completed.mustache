<div class="local_datahub_completed" id="">
    <h3>{{# str }} completedtitle, local_datahub {{/ str }}</h3>
    <p>{{# str }} completeddesr, local_datahub {{/ str }}</p>
    <!-- Buttons to pause and continue processing. Enable/disable draggables. -->
    <div class="datepicker" id="datepicker_parent">
        <input type="date" class="startdate" id="completed_startdate" pattern="[0-9]{2}/[0-9]{2}/[0-9]{4}" value="" min="" max=""></input>
        <span class="separator"> - </span>
        <input type="date" class="enddate" id="completed_enddate" pattern="[0-9]{4}-[0-9]{2}-[0-9]{2}" value="" min="" max=""></input>
        <button type="button" class="btn" id="completed_refresh">
            Refresh
            <i class="fa fa-refresh"></i>
        </button>
    </div>
    <!-- Alert for when pauses, continues, etc. fail. -->
    <div id="" class="alert alert-danger alert-dismissible fade queue_alert" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <p id="queue_alert_msg" class="queue_alert_msg"></p>
    </div>
    <!-- Alert for when pauses, continues, etc. succeed. -->
    <div id="" class="alert alert-success alert-dismissible fade queue_success" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <p id="queue_success_msg" class="queue_success_msg"></p>
    </div>
    <!-- Table to contain jobs queue. -->
    <table class="table completed-jobs" id="completed_job_table">
        <thead class="thead-default">
            <tr class="primary-header">
                <th colspan="1" class="col-filename">{{# str }} queuefilename, local_datahub {{/ str }}</th>
                <th colspan="1" class="col-status">{{# str }} queuestatusname, local_datahub {{/ str }}</th>
                <th colspan="1" class="col-status-details">{{# str }} completeddate, local_datahub {{/ str }}</th>
                <th colspan="1" class="col-options"></th>
            </tr>
        </thead>
        <tbody class="insert-inside">
        </tbody>
    </table>
    <div class="loading" id="loading_indicator">
        <i class="fa fa-spinner fa-spin fa-4"></i>
    </div>
</div>
<div id="timezone_alert" class="timezone-notify">
    <p></p>
</div>
